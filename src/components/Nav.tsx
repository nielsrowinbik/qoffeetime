import styled from "styled-components";

export const NavHeading = styled.h1`
    font-family: inherit;
    font-size: 1.4rem;
    margin-left: 24px;
`;

export const Nav = styled.nav`
    align-items: center;
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    grid-area: nav;
    height: 100%;
    margin: 0 6px;
`;
