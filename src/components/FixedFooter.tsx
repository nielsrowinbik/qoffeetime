import styled from "styled-components";

export const FixedFooter = styled.footer`
    bottom: 0;
    display: flex;
    left: 0;
    position: fixed;
    right: 0;

    > * {
        flex: 100%;
    }
`;
