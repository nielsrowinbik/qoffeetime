import styled from "styled-components";

export const RecipeSlidesWrapper = styled.div`
    display: grid;
    grid-template-rows: auto min-content;
    grid-area: nav / nav / main / main;
    height: 100%;
    overflow-x: hidden;
    padding: 16px 0 0;
`;
