import styled from "styled-components";

export const Nav = styled.nav`
    align-items: center;
    display: flex;
    flex: 0 0 64px;
    flex-direction: row;
    flex-wrap: nowrap;
    height: 64px;
`;
