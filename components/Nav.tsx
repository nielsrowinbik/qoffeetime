import styled from "styled-components";

export const Nav = styled.nav`
    align-items: center;
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    grid-area: nav;
    height: 64px;
`;
